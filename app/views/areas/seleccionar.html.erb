{
  areas_dependientes: <%= @area.all_children(:conditions => {:activo => true}).inject([]){|arr, v| arr << v.nombre}.to_json %>,
  parent: '<%= @area.buscar_parent %>',
  nivel: '<%= @area.nivel %>',
  tipo: '<%= @area.tipo %>'
}
